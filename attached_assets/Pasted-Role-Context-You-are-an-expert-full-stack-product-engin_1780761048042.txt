Role & Context:
You are an expert full-stack product engineer and technical architect building "Cerevia," a migraine consultation-intelligence platform. We are building this for a hackathon. The output must be serious, startup-grade, and fully functional for judges to test.  Core Objective:
Transition from the deterministic "Sarah" demo narrative to a functional, multi-user system where fresh GP and Patient accounts can be created and linked via an access-code loop.  I. Database & Auth Requirements:Use the relational schema defined in the Development Guide (profiles, patients, gp_profiles, gp_access, etc.).  Implement Supabase Auth (Email/Password). On signup, use a database trigger to create a corresponding row in the patients or gp_profiles table based on the user's role selection.  II. The "Test Patient" Toggle:In the Patient Dashboard UI, add a "Demo Mode" toggle that populates the fresh account with 12 weeks of seeded data (following the Sarah Ahmed narrative: increasing frequency, variable sumatriptan effectiveness, and triggers like poor sleep/screen time).  This toggle should insert mock migraine_events and wearable_events into the current user's patient_id so judges can see the GP dashboard light up immediately.  III. GP-Patient Linking (The Core Demo Loop):Patient Share UI: Build a component that generates a short access code (e.g., CER-4821), inserts it into the gp_access table with status='active', and displays it to the patient.  GP Link UI: Build a GP-side input to enter the access code. On submit, update the gp_access row to set gp_id to the current GP profile.Data Security: Implement RLS policies ensuring that a GP can only SELECT data from migraine_events and medication_logs if they have an active row in the gp_access table linking them to that patient.  IV. Development Guidelines (Non-negotiable):No Hardcoding: Do not hardcode the patient "Sarah". Everything must be dynamic based on the logged-in user's ID.  Safe Wording: AI/UI outputs must use clinical-safe language ("reported", "appears", "may be worth discussing") and include the mandatory safety disclaimer.  Tech Stack: Use Next.js App Router, Tailwind CSS, Supabase JS client, and Recharts/Three.js for visualizations.  Wait for Branding: Do not finalize colors, typography, or shadows until I provide the Branding Guidelines prompt. Keep the architecture clean.  Task:Start by outlining the Supabase SQL setup for the GP-Patient linking and the Patient Dashboard "Demo Mode" seeder logic. Once confirmed, we will move to the UI components.