Role & Context:
You are an expert Next.js and Supabase full-stack engineer building the "Cerevia" hackathon demo.  Current Task: Implement Fresh Account Routing & GP-Patient Linking
We are deprecating the hardcoded/deterministic demo patient ("Sarah"). We need the application to support fresh, empty accounts for new signups and implement a secure "Share with GP" linking feature.  Step 1: Fresh Account RoutingOn login or signup via Supabase Auth, verify the user's role from the profiles table.  If role is 'patient', route them to an empty patient dashboard and ensure a record exists in the patients table.  If role is 'gp', route them to an empty GP dashboard and ensure a record exists in the gp_profiles table.  Step 2: Patient UI ("Share with GP")Build a "Share with GP" UI component for the patient dashboard.  When clicked, generate a unique access code (e.g., CER-4821).  Insert a row into the gp_access table:  patient_id = current patient's ID.  gp_id = NULL (this is the pending state).access_code = the generated code.  access_status = 'active'.  expires_at = current timestamp + 7 days.  Display the code and include a "Revoke Access" button that sets access_status to 'revoked'.  Step 3: GP UI ("Link Patient")Build a UI component for the GP dashboard labeled "Enter Patient Access Code".On submit, query gp_access where access_code matches and access_status is 'active'.If found, update that row, setting gp_id to the current GP's profile ID.The GP dashboard should then update to display this patient, allowing the GP to click through to their data.Step 4: RLS & Data FetchingUpdate all Supabase queries on the GP dashboard to filter migraine_events, medication_logs, etc., by checking that a valid row exists in gp_access linking the current GP and the patient.Ensure Supabase Row Level Security (RLS) policies are configured so that GPs can only SELECT data for patients who have linked to them via an active gp_access row.Deliverables:Updated Patient "Share" UI component.Updated GP "Link" UI component.Updated data-fetching logic for both dashboards.SQL for the necessary RLS policies to secure the data link.