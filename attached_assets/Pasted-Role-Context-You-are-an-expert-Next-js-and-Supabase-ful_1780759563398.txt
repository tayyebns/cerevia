Role & Context:
You are an expert Next.js and Supabase full-stack engineer building the "Cerevia" hackathon demo.  Current Task: Live Account Routing & GP-Patient Linking
We are removing the hardcoded/deterministic demo patient ("Maria" or "Sarah"). We need the app to support fresh, empty accounts for new signups and implement the "Share with GP" linking feature.  Step 1: Fresh Account RoutingUpon login or signup via Supabase Auth, check the user's role in the profiles table.If they are a patient, route them to an empty patient dashboard. Ensure a corresponding row exists in the patients table.If they are a gp, route them to an empty GP dashboard. Ensure a corresponding row exists in the gp_profiles table.Step 2: Patient Flow ("Share with GP")Build a UI component on the Patient dashboard called "Share with GP".  When clicked, generate a short, readable access code (e.g., CER-4821).  Insert a new row into the gp_access table:  patient_id = the current user's patient ID.  gp_id = NULL (pending claim).access_code = the generated code.  access_status = 'active'.  expires_at = 7 days from now.  Display this code to the patient with a "Revoke Access" option.  Step 3: GP Flow ("Link Patient")Build a UI component on the GP dashboard to "Enter Patient Access Code".When the GP submits a code, query the gp_access table for that access_code where access_status = 'active'.If found, update that row by setting the gp_id to the current GP's profile ID.The GP's dashboard should now list this patient and allow the GP to click into their detailed timeline and Migraine Twin data.Step 4: Data Fetching & RLSUpdate all Supabase queries on the GP dashboard so they only fetch migraine_events, medication_logs, migraine_triggers, etc., for patients who have an active link with the GP in the gp_access table.Write and apply the necessary Supabase Row Level Security (RLS) policies to enforce this. GPs must only be able to SELECT data for patients they are linked to.Please output the updated components for the Patient Share UI, the GP Link UI, the updated dashboard data-fetching logic, and the SQL for the updated RLS policies.